// Create the element, show entries for the current webpage.
var sidebarElement = new google.elements.sidewiki.SidebarElement(window.location.href, true);

// Bind to element.
sidebarElement.bindToElement("sidewiki-element");